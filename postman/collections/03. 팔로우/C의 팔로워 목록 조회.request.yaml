$kind: http-request
name: C의 팔로워 목록 조회
url: "{{baseUrl}}/api/v1/follows/{{creatorC_id}}/followers"
method: GET
headers:
  Authorization: "Bearer {{userA_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 200\", function () { pm.response.to.have.status(200); });"
    language: text/javascript
order: 3000
