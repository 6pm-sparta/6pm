$kind: http-request
name: A 대기열 진입
url: "{{baseUrl}}/api/v1/tickets/shows/{{showId}}/queue"
method: POST
headers:
  Authorization: "Bearer {{userA_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 200\", function () { pm.response.to.have.status(200); });"
    language: text/javascript
order: 1000
