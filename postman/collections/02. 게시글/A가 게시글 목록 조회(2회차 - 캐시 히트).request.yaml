$kind: http-request
name: A가 게시글 목록 조회(2회차 - 캐시 히트)
url: "{{baseUrl}}/api/v1/feeds/posts"
method: GET
headers:
  Authorization: "Bearer {{userA_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 200\", function () { pm.response.to.have.status(200); });"
    language: text/javascript
order: 3000
