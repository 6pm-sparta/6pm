$kind: http-request
name: "A 게시글목록조회 1회차 (응답시간 기록 - 캐시 미스 예상)"
url: "{{baseUrl}}/api/v1/feeds/posts"
method: GET
headers:
  Authorization: "Bearer {{userA_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 200\", function () { pm.response.to.have.status(200); }); pm.collectionVariables.set(\"firstCallTime\", pm.response.responseTime); console.log(\"1차 조회 응답시간(ms):\", pm.response.responseTime);"
    language: text/javascript
order: 2000
