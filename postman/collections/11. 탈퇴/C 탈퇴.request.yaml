$kind: http-request
url: "{{baseUrl}}/api/v1/members/me"
method: DELETE
headers:
  Authorization: Bearer {{creatorC_token}}
scripts:
  - type: afterResponse
    code: pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
